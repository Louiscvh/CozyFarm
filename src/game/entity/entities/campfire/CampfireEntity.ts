import type { Entity } from "../../Entity"

export const CampfireEntity: Entity = {
  id: "campfire",
  model: "procedural:campfire",
  modelSize: 1,
  yOffset: -0.05,
  castShadow: false,
  receiveShadow: false,
}
